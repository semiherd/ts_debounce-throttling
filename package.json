{
  "name": "debounce_throttle",
  "version": "1.0.0",
  "description": "Small debounce utility (TypeScript)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && jest --colors",
    "demo:bundle": "esbuild index.ts --bundle --platform=browser --format=esm --outfile=demo/lib.mjs --log-level=warning",
    "demo:serve": "http-server -c-1 demo -p 8080"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "esbuild": "^0.19.0",
    "http-server": "^14.1.1"
  }
}
